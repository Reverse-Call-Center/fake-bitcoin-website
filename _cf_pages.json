{
  "version": 1,
  "build": {
    "command": "cd Client && npm install && npm run build",
    "outputDirectory": "Client/dist"
  },
  "routes": [
    {
      "src": "/.*",
      "dest": "/index.html"
    }
  ]
}
